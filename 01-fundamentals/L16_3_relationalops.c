// Relational operators: == , != , <= , >= , < , >

#include <stdio.h>

int main()
{
    if (4 <= 5)
    {printf("4 is lesser than or equal to 5.\n");}
    else
    {printf("4 is not lesser than or equal to 5.\n");}
}